name: kibana

#dependencies:
#  - elasticsearch

# list of files and directories listed that are copied into project .ddev directory
project_files:
- docker-compose.kibana.yaml

global_files:
  - commands/host/kibana
